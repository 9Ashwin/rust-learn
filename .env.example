# 环境变量配置模板
# 复制为 .env 后修改: cp .env.example .env
# .env 不会被提交到 git

# 服务器地址 (覆盖 config.toml 的 server.addr)
APP_SERVER_ADDR=127.0.0.1:3000

# 数据库连接 (覆盖 config.toml 的 database.url)
APP_DATABASE_URL=sqlite:data.db?mode=rwc

# 日志级别 (tracing 读取)
RUST_LOG=rust_learn=info,tower_http=info
